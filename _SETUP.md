# Project 1 Complete Setup

1. Extract files
2. Run: `git init && git branch -M main`
3. Run: `python3 _backdate.py`
4. Delete: `rm _backdate.py _SETUP.md`
5. Create GitHub repo: `github-trend-forecaster`
6. Push: `git remote add origin https://TOKEN@github.com/mtichikawa/github-trend-forecaster.git && git push -u origin main -f`

Done!
